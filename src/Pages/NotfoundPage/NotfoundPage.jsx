import React from 'react';
import { wrapper, info } from './NotfoundPage.module.scss';

const NotfoundPage = () => (
  <div className={wrapper}>
    <div className={info}>Страница не найдена</div>
  </div>
);

export default NotfoundPage;
